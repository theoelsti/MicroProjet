 Pour le debut :
    - On veut les 10 dernieres valeurs
    - On récupere dans l'ordre :
        la timeScale
        on push le tableau
        on recuepere la temp
        on push le tableau
        on recuepere l'hum
        on push le tableau
    on ferme la connexion
    on print toutes les valeur sous la forme
    let temp  = [10,12,15,12,13,16,17,18,16];
    let hum = [56,75,74,70,68,50,62,45,56];
    let timeScale = ['1h', '50min', '40 min', '30 min', '20 min', '10 min', '5 min', '1 min'];


        