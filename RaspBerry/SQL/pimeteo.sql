CREATE TABLE IF NOT EXISTS pimeteo (
              `date` datetime NOT NULL,
              temp decimal(3,1) NOT NULL, 
              hum decimal(4,1) NOT NULL, 
              res decimal(3,1) NOT NULL);